# Config file path (defaults to {CARGO_MANIFEST_DIR}/config.toml if not set)
# BOTS_CONFIG=apps/bots/config.toml

# Exchange URL (overrides config.exchange.url from config.toml)
EXCHANGE_URL=http://localhost:8888

# All other settings are configured in config.toml
# You can override any config value using BOTS_ prefixed env vars, e.g.:
# BOTS_ACCOUNTS_MAKER_ADDRESS=custom_maker
# BOTS_ORDERBOOK_MIRROR_ENABLED=false